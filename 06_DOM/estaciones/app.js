// Usando un input de tipo date, permitir seleccionar una fecha, y que el color de fondo y el texto que haya en la página cambien para indicar la estación del año de la fecha. 

const dateLabel = document.getElementById("dateLabel");
const dateInput = document.getElementById("dateInput");
